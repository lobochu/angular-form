export class Fruit {
    id: number;
    name: string;
    color: string;
    shape: string;

    constructor(id, name, color, shape) {
        this.id = id;
        this.name = name;
        this.color = color;
        this.shape = shape;
    }
}
